ultralytics>=8.3
supervision>=0.25,<0.30
opencv-python>=4.10
numpy>=1.26
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
librosa>=0.10
soundfile>=0.12
imageio-ffmpeg>=0.5
pyyaml>=6.0
yt-dlp
# Windows-only wheels, no sdist: without the marker, `pip install -r`
# fails outright on Linux and macOS before anything can run. The code
# already imports it defensively (engine/audio_live.py).
pyaudiowpatch>=0.2.12; sys_platform == "win32"
shazamio

# Imported directly rather than only through ultralytics:
#   engine/detector.py    import torch        (unguarded, top level)
#   engine/audio.py       import pydub        (guarded, optional path)
torch
pydub
